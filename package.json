{
  "name": "chrome_extension",
  "version": "0.0.4",
  "scripts": {
    "lint": "echo 'do some linting'",
    "test": "echo 'run unit test'",
    "publish": "node set_version.js && zip -r src.zip src && sh publish.sh"
  }
}
